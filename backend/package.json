{
  "name": "bluconnet-backend",
  "version": "1.0.0",
  "description": "BluConnet Media — AI Assistant backend (leads, meetings, careers, chat history, analytics, notifications)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "migrate": "node database/migrate.js",
    "contacts:verify": "node database/verify.js",
    "news:images:migrate": "node database/migrateNewsImages.js",
    "test": "node --test \"tests/*.test.js\"",
    "build": "node -e \"console.log('backend: no build step (plain Node.js, nothing to compile)')\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cloudinary": "^2.11.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.2.0",
    "multer": "^2.0.1",
    "mysql2": "^3.24.4",
    "nodemailer": "^6.9.13",
    "ssh2": "^1.17.0"
  }
}
